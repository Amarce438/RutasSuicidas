import { firebaseConfig } from '../config/firebase.config';

// Firebase se inicializa automáticamente con react-native-firebase
// Este archivo asegura que la configuración esté disponible

export { firebaseConfig };